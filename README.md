
# Análise de Normalidade com Python

Este repositório contém um script em Python (`main.py`) para análise de normalidade de dados, aplicando testes estatísticos e visualizações gráficas. O objetivo é verificar se diferentes conjuntos de dados seguem uma distribuição normal.

## 🧪 Pré-requisitos

Instale as dependências:

```bash
pip install numpy matplotlib scipy
```

## ▶️ Como executar

No terminal, execute:

```bash
python main.py
```
